@model IEnumerable<ProjetoReceitas.Models.NivelDificuldade>

<h2>Index</h2>

<p>
    @Html.ActionLink("Novo", "Cadastrar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Editar", "Editar", new { id=item.DificuldadeId }) |
            @Html.ActionLink("Remover", "Remover", new { id=item.DificuldadeId })
        </td>
    </tr>
}

</table>
